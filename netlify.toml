[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router — redirect all routes to index.html (prevents 404 on refresh)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
